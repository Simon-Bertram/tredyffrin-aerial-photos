---
import { LocationPhotoCard as LocationPhotoCardUi } from "@/components/location/location-photo-card";
import type { LocationPhoto } from "@/lib/locations";

interface Props {
  photo: LocationPhoto;
}

const { photo } = Astro.props;
---

<LocationPhotoCardUi photo={photo} layout="grid" />
